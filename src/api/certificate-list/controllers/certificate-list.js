'use strict';

/**
 * certificate-list controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::certificate-list.certificate-list');
